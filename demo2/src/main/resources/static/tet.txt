用户模块
	user_type		//用户类型表
		id							int				:主键id
		type_name					varchar(20)		:类型名称                   优质
		score                       int             :积分                        1000
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

	user			//用户表
		id							int				:主键id
		user_name					varchar(20)		:用户名称
		user_age					int 			:用户年龄
		user_phone					varchar(13)		:用户电话
		user_email					varchar(20)		:用户邮箱
		user_type					int				:用户类型               2
		user_score					int				:用户积分               500
		id_card  					varchar(19)		:用户身份证
		status						int				:用户是否可用
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

权限模块
	role			//角色表
		id							int				:主键id
		role_name					varchar(20)		:角色名称
		status						int 			:角色是否可用
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

	acl_module		//权限模块表
		id 							int				:主键id
		acl_module_name				varchar(20)		:权限模块名称
		status						int				:权限模块是否可用
		seq							int				:默认排序顺序
		parent_id					int				:父权限模块id
		level						varchar(100)	:层级
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

	acl				//权限表
		id 							int				:主键id
		acl_name 					varchar(20)		:权限名称
		status						int				:权限是否可用
		seq 						int				:默认排序顺序
		type						int				:权限是什么类型
		url							varchar(50)	:权限url
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

	role_acl		//角色权限表
		id 							int				:主键id
		acl_id 						int				:权限id
		role_id						int				:角色id
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

	role_admin		//角色管理员表
		id 							int				:主键id
		acl_id 						int				:权限id
		admin_id					int				:角色id
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

书籍模块
	boot_type		//书籍类型表
		id							int				:主键id
		type_name					varchar(20)		:类型名称
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

	book_lease_type	//书籍租借类型
		id							int				:主键id
		type_name					varchar(20)		:类型名称
		score						int				:租借所需积分
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark

	book_loss_type	//书籍损坏类型表
		id							int					:主键id
		type_name					varchar(20)			:类型名称
		score						int					:扣多少分
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark

	book_loss		//书籍损坏表
		id							int					:主键id
		book_id						int					:图书id
		user_id						int					:用户id
		status						int					:损坏程度
		operator					varchar(20)			:创建管理员
		operate_time				date				:创建时间
		remark						varchar(200)		:备注

	book_lease		//书籍租借表
		id							int					:主键id
		book_id						int					:图书id
		user_id						int					:用户id
		status						int					:租借状态

		operator					varchar(20)			:创建管理员
		operate_time				date				:创建时间
		remark						varchar(200)		:备注

	book			//书籍表
		id							int					:主键id
		book_name					varchar(40)			:书籍名称
		book_code					varchar(40)			:书籍编号
		author_name					varchar(20)			:作者名称
		press_name					varchar(20)			:出版社名称
		book_type_id				int					:书籍类型id
		book_lease_id				int					:书籍租借id
		book_loss_id				int					:书籍损耗id
		book_lease_type				int					:书籍租借类型id
		book_space_id				int					:书籍空间id
		operator					varchar(20)			:创建管理员
		operate_time				date				:创建时间
		remark						varchar(200)		:备注

管理员模块
	admin			//管理员表
		id 							int				:主键id
		admin_code                  varchar(20)     :登录账号
		admin_pwd                   varchar(20)     :登录密码
		admin_name					varchar(20)		:管理员名称
		admin_role					int				:管理员角色
		admin_age					int				:管理员年龄
		id_card						varchar(19)		:管理员身份证
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

空间模块
	space			//空间表
		id							int				:主键id
		space_name					varchar(20)		:空间名称
		parent_id					int				:父空间
		level						varchar(50)		:层级
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注

日志模块
	log				//日志表
		id							int				:主键id
		type						int				:日志类型
		targetId					int				:目标主键
		old_value					text			:旧的值
		new_value					text			:新的值
		operator					varchar(20)		:创建管理员
		operate_time				date			:创建时间
		remark						varchar(200)	:备注